import type { GenerationConfiguration } from "/GenerationConfiguration"

/**
 * Context object passed through the generation process.
 */
export type GenerationContext = {
  configuration: GenerationConfiguration
}
